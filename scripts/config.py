#!/usr/bin/python

RPPATH = "http://localhost:8000"

# Status
STATUS = "{}/status".format(RPPATH)

# Data
DATA = "{}/data".format(RPPATH)

# More verbose http requests
DEBUG = True
HTTP_DEBUG = True

